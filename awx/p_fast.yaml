---
- name: "run fast for notification {{ ansible_play_hosts|to_yaml }}"
  hosts: "{{ group_name | default('mn1') }}"
  gather_facts: true
  any_errors_fatal: true
  ignore_unreachable: false
  debugger: never
  become: true
  vars:
    pl_name: "p_fast.yml"

##############################################

  pre_tasks:
    - block:
        - name: "set special stat"
          ansible.builtin.include_role:
            name: r_set_state
          vars:
            dict_results: {'varName': 'pre_tasks', 'varValue': 'pre_tasks'}
            dict_messages: {'type': 'pre_tasks', 'action': 'pre_tasks', 'message': 'pre_tasks'}
            dict_overallStatus: {'status': 'pre_tasks', 'message': 'pre_tasks'}

        - name: "build error"
          ansible.builtin.debug:
            msg: "{{ error }}"
          failed_when: error == "1"

      rescue:
        - name: "pre tasks {{ pl_name }} failed"
          ansible.builtin.include_role:
            name: r_set_state
          vars:
            dict_results: {'varName': 'pre_tasks failed', 'varValue': 'pre_tasks failed'}
            dict_messages: {'type': 'pre_tasks failed', 'action': 'pre_tasks failed', 'message': 'pre_tasks failed'}
            dict_overallStatus: {'status': 'pre_tasks failed', 'message': 'pre_tasks failed'}

        - name: "pre tasks {{ pl_name }} failed"
          ansible.builtin.fail:
            msg: "running rollback"
  
      tags:
        - always

##############################################

  tasks:
    - block:
        - name: "set special stat"
          ansible.builtin.include_role:
            name: r_set_state
          vars:
            dict_results: {'varName': 'tasks', 'varValue': 'tasks'}
            dict_messages: {'type': 'tasks', 'action': 'tasks', 'message': 'tasks'}
            dict_overallStatus: {'status': 'tasks', 'message': 'tasks'}
          tags:
            - set_state

      rescue:
        - name: "tasks {{ pl_name }} failed"
          ansible.builtin.fail:
            msg: "running rollback"

##############################################

  post_tasks:
    - block:
        - name: "set special stat"
          ansible.builtin.include_role:
            name: r_set_state
          vars:
            dict_results: {'varName': 'post_tasks', 'varValue': 'post_tasks'}
            dict_messages: {'type': 'post_tasks', 'action': 'post_tasks', 'message': 'post_tasks'}
            dict_overallStatus: {'status': 'post_tasks', 'message': 'post_tasks'}

      rescue:
        - name: "post tasks {{ pl_name }} failed"
          ansible.builtin.fail:
            msg: "running rollback"

      tags:
        - always

##############################################
